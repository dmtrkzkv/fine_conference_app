# data_requirements_ecio2026.txt — ECIO 2026
#
# Structured manifest of the data files that the fetch (downloader) and process
# scripts in this directory rely on. make_app.py reads this file to decide
# whether a download can be skipped: when FORCE_DOWNLOAD is off, every file
# marked "required = yes" below must already exist in data/ for the download to
# be skipped; otherwise make_app runs the downloader.
#
# Format (simple, line-oriented; '#' starts a comment, blank lines ignored):
#   - Each file is a block introduced by a header line "[file: <pattern>]".
#   - <pattern> is a filename or a glob resolved relative to the data/
#     subdirectory of THIS conference folder.
#   - Inside a block, "key: value" lines give metadata. Recognized keys:
#       required     yes | no   (default yes) — whether the file must be present
#       produced_by  which script normally creates it (fetch / process / manual)
#       description  one-line human description
#       manual       how to obtain the file by hand if the downloader can't
#   - Continuation: a "manual" value may wrap onto following indented lines.
#
# manual_steps_written: 2026-05-29
# These manual-download instructions were accurate as of the date above; the
# conference website / API can change, so re-verify if much later.

generated_for: ecio2026
manual_steps_written: 2026-05-29

[file: ECIO2026_program.json]
required: yes
produced_by: fetch
description: Full ECIO 2026 program as a single JSON document — every session
  and every talk, with titles, times, rooms, presider/speaker names,
  affiliations, tags (Invited / Plenary / Special Event), and per-talk
  abstracts. Pulled from Optica's presentations API
  (https://www.optica.org/api/presentations/?EventAcronym=ECIO&PageSize=500),
  which is the same endpoint the public on-line schedule page uses.
manual: Open https://www.optica.org/events/topical_meetings/european_conference_on_integrated_optics_(ecio)/schedule/
  in a browser (this sets the bot-protection cookies), then open
  https://www.optica.org/api/presentations/?EventAcronym=ECIO&PageSize=500
  in the same browser session and save the resulting JSON into this folder's
  data/ subdirectory as exactly ECIO2026_program.json . (If the API endpoint
  ever changes URL or query-parameter casing, re-derive both from the
  data-endpoint attribute on the schedule page's #events-schedule-app div.)
